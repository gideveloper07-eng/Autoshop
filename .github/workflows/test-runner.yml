name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Show Server Name
        run: hostname

      - name: Show Current Directory
        run: pwd

      - name: Show Date
        run: date
